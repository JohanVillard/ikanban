{
  "name": "ikanban",
  "version": "1.0.0",
  "scripts": {
    "start:dev": "docker-compose --env-file ./backend/.env.development -f docker-compose.dev.yml up --build",
    "start:prod": "docker-compose --env-file ./backend/.env.production -f docker-compose.prod.yml up --build"
  }
}
