export type ColumnDbRecord = {
    id: string;
    board_id: string;
    name: string;
    position: number;
    wip: number;
};

export type Column = {
    id: string;
    boardId: string;
    name: string;
    position: number;
    wip: number;
};
